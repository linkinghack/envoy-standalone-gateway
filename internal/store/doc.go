// Package store persists management-plane runtime state in SQLite.
//
// Configuration source files are deliberately not stored here; the filesystem
// remains the source of truth for drafts and version snapshots.
package store
