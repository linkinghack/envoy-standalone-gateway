# T3：协议表单与 YAML 双向编辑

## 目标

让六种协议对象可通过 schema 驱动表单完整 CRUD，并与 YAML 文件真源无损切换。

## 步骤

1. schema resolver/form engine；2. object list/detail/create/delete；3. variant/array/scalar 与 RawJSON fallback；4. resourceVersion 冲突；5. round-trip tests。

## 进展

- 待开始。
