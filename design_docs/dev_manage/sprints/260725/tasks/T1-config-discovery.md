# T1：配置与 Envoy 发现

## 目标

落地 `deliver.static.*`、`proc.*` 严格配置及二进制发现/版本门禁，为所有后续进程动作建立确定输入。

## 步骤

1. 配置结构、默认值和跨字段安全校验；
2. 环境/显式/PATH 发现顺序；
3. `envoy --version` 超时执行和语义版本解析；
4. 低版本拒绝、高版本告警；
5. 表驱动测试并回写。

## 进展

- 2026-07-22：完成。`deliver.static.outputPath`、`proc.*` 默认值与跨字段安全约束已纳入 strict config；托管缺省关闭。
- Envoy 发现按环境→配置→PATH，`--version` 有界执行；1.37~1.39 接受、低版本拒绝、高版本告警，单测覆盖优先级/超时/解析/边界。
