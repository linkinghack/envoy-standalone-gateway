# T1：M-STORE SQLite migration 与基础 CRUD

## 目标

实现无 CGO 的 SQLite 运行态存储，完成 schema migration、settings 读写和版本序号/元数据落库。

## 步骤

1. 引入 `modernc.org/sqlite`；
2. 实现 `internal/store`；
3. 覆盖 migration 幂等、settings、版本序号和约束测试；
4. 运行全量检查并回写本文件。

## 进展

- 2026-07-20：已完成。实现 `internal/store`，SQLite migration、settings、版本序号/元数据和 active publish 唯一索引均有测试；`make build test lint` 全绿。
