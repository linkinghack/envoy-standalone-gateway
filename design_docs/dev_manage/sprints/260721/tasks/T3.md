# T3：版本快照与冲刺收口

## 目标

实现不可变版本快照的原子落盘，并完成本轮全量验证。

## 进展

- 2026-07-20：已完成。`conf.Snapshot` 使用 `tmp` 临时目录、原样复制真源、写入 `meta.json` 后 rename 到 `versions/%06d`；`conf.Publisher.Publish` 串起编译、快照、Deliverer.Apply 和 effective/failed 留痕；重复序号拒绝覆盖。`make build test lint` 全绿。
